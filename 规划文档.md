# 中国市场汽车动力总成抱怨自动采集与分析系统 — 规划文档

> 目标：自动抓取中国市场汽车动力总成（Powertrain）相关客户抱怨（Complaint / VOC），
> 结构化存储，支持按品牌/车系/子系统维度分析，为质量管理与产品改进提供输入。

---

## 1. 需求澄清（请确认）

| # | 决策点 | 选项 | 建议 |
|---|--------|------|------|
| 1 | 品牌覆盖范围 | A. 全品牌全车系；B. 指定品牌/车系清单 | **A（先全量采集，分析时再过滤）**，避免漏报 |
| 2 | 抓取深度 | A. 仅列表页字段（标题/代码/时间）；B. 进详情页抓完整投诉内容 | **B**，抱怨的语义分析（故障模式、严重度、诉求）依赖全文 |
| 3 | 历史数据回填 | A. 仅从今天起增量；B. 回填历史数据做基线 | 建议**先增量（近 7 天）跑通，后续按需回填**历史做趋势基线 |
| 4 | 输出形态 | Excel 报表 / SQLite 库 / 飞书推送 / 网页看板（可多选） | M1 先 **CSV/SQLite**，M3 加 **Excel 报表 + 飞书日报** |
| 5 | 采集频率 | 每日 / 每周 | **每日**（车质网日增量约几十~百余条，全量抓取成本低） |
| 6 | 动力总成口径 | 燃油（发动机/变速器/离合器）+ 新能源（三电/混动）全覆盖？ | **全覆盖**，分子系统打标（见 §5） |

---

## 2. 数据源分析（已实测验证）

### 主源：车质网（12365auto.com）— 国内最权威的汽车投诉平台

| 验证项 | 实测结果 |
|--------|----------|
| 列表页可达性 | HTTP 200，约 68KB/页，GBK 编码 |
| 分页 URL | `https://www.12365auto.com/zlts/0-0-0-0-0-0_0-0-1.shtml`（末位为页码） |
| 列表页字段 | 投诉编号、品牌、车系、车型、问题简述（含详情链接）、**典型问题代码 `data-cti`**、投诉时间、投诉状态 |
| 详情页字段 | 投诉编号、时间、品牌、车系、车型、**完整投诉内容**（`div.tsnr`）、投诉回复（`div.tshf`） |
| 分类 URL 筛选位 | ❌ 实测不生效（各页内容混合），**必须靠 `data-cti` 代码 + 关键词双通道识别** |
| 反爬现状 | 当前无强反爬（普通 UA 即可访问），仍需限速与重试策略以防触发封禁 |

### 备选源（M4 扩展）
- **缺陷产品管理中心**（dpac.org.cn）：召回/缺陷公告，权威但当前直连不可达（需换域名或代理）
- **汽车之家 / 懂车帝**：论坛讨论，需较强反爬对抗，作为舆情补充
- **黑猫投诉**：跨品类，动力总成占比低，优先级最低

---

## 3. 架构设计

```
                    ┌─────────────────────────────────────────────┐
                    │                 调度层（cron / launchd）      │
                    └──────────────────┬──────────────────────────┘
                                       ▼
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│ 采集层    │ →  │ 解析层    │ →  │ 识别层    │ →  │ 存储层    │ →  │ 输出层    │
│ crawler  │    │ parser   │    │ classifier│    │ storage  │    │ report   │
│ 列表+详情 │    │ 字段抽取   │    │ 动力总成   │    │ SQLite   │    │ CSV/Excel│
│ 限速重试  │    │ 清洗规范化 │    │ 子分类打标 │    │ 去重幂等   │    │ 飞书通知  │
└──────────┘    └──────────┘    └──────────┘    └──────────┘    └──────────┘
                                                                    │
                                                    ┌───────────────▼───────────────┐
                                                    │  分析：按品牌/车系/子系统聚合、 │
                                                    │  趋势、TOP 问题排行、词频       │
                                                    └───────────────────────────────┘
```

**设计原则**
- **分层解耦**：采集/解析/识别/存储/输出各自独立，页面改版只需改 parser，识别规则调整只动配置
- **幂等与去重**：以车质网"投诉编号"为主键，重复运行不产生脏数据
- **增量优先**：默认只抓"新出现的投诉编号"，历史已在库则跳过
- **可配置规则**：动力总成识别规则（代码映射 + 关键词表）全部外置到配置文件，业务可直接维护

---

## 4. 数据模型（SQLite）

```sql
CREATE TABLE complaints (
    id            INTEGER PRIMARY KEY AUTOINCREMENT,
    complaint_no  TEXT UNIQUE NOT NULL,        -- 车质网投诉编号（去重主键）
    brand         TEXT,                        -- 品牌
    series        TEXT,                        -- 车系
    model         TEXT,                        -- 车型（含年款/配置）
    title         TEXT,                        -- 问题简述
    content       TEXT,                        -- 完整投诉内容（详情页）
    issue_code    TEXT,                        -- 典型问题代码 data-cti（如 A191）
    issue_text    TEXT,                        -- 典型问题描述（由代码映射）
    complaint_date TEXT,                       -- 投诉日期
    status        TEXT,                        -- 投诉状态（信息审核/厂家受理…）
    detail_url    TEXT,                        -- 详情页链接
    -- 动力总成识别结果
    is_powertrain INTEGER DEFAULT 0,           -- 1=动力总成相关
    pt_subsystem  TEXT,                        -- 子系统标签（见 §5）
    matched_by    TEXT,                        -- 命中通道：code / keyword / both
    matched_keywords TEXT,                     -- 命中的关键词（逗号分隔，便于追溯）
    -- 元数据
    crawled_at    TEXT DEFAULT (datetime('now','localtime'))
);
CREATE INDEX idx_pt ON complaints(is_powertrain, pt_subsystem);
CREATE INDEX idx_date ON complaints(complaint_date);
```

---

## 5. 动力总成识别规则（核心难点）

**双通道识别 + 可配置映射**，解决"分类 URL 不生效"与"新能源转型带来的口径漂移"问题。

### 5.1 通道一：典型问题代码（data-cti，权威信号）

车质网代码首字母对应大系统，建立白名单映射（M1 阶段用抽样验证校准，存配置）：

| 前缀 | 系统 | 动力总成相关 |
|------|------|:---:|
| A | 发动机（含冷却/润滑/增压/正时/排放） | ✅ 是 |
| B | 变速器 | ✅ 是 |
| C | 离合器 | ✅ 是 |
| N / H 部分项 | 新能源三电（电池 BMS、电机电驱、电控） | ✅ 是（需白名单子集） |
| D/E/F/G/I/J/K/L/M 等 | 转向/制动/悬架/车身/空调/内饰/服务 | ❌ 否 |

> 代码→子系统映射示例：`A191→发动机/冷却系统`、`H343→动力电池/BMS`。
> 完整映射表在 M1 用实测数据校准后固化到 `config/issue_codes.json`。

### 5.2 通道二：标题 + 内容关键词规则（语义信号，防漏判）

| 子系统 | 关键词（同义词表，可扩展） |
|--------|--------------------------|
| 发动机 ENGINE | 发动机、引擎、机油、烧机油、漏油、增压器、正时、缸体、拉缸、高温、过热、熄火、失速、无法启动、抖动 |
| 变速器 TRANSMISSION | 变速器、变速箱、顿挫、异响、无法换挡、挂挡、跳挡、漏油、拖挡、CVT、双离合、AT |
| 离合器 CLUTCH | 离合器、离合片、打滑、分离不彻底 |
| 动力电池 EV_BATTERY | 电池、续航、衰减、充不进、充电故障、BMS、热失控、起火、鼓包、掉电 |
| 驱动电机 EV_MOTOR | 电机、电驱、异响、啸叫、失去动力 |
| 电控 EV_CONTROL | 电控、控制器、整车断电、系统故障 |
| 混动 HYBRID | 混动、亏电、保电、HEV、PHEV、增程、失速 |

**排除规则（黑名单）**：防服务类误报，如"承诺的充电桩不予兑现"（销售服务）虽含"充电桩"但非动力总成——用 `exclude_keywords` 表处理。

### 5.3 判定逻辑

```
is_powertrain = (issue_code ∈ 白名单) OR (标题/内容命中关键词)
subsystem     = 代码映射优先；代码未命中时按关键词归类（可多标签，取首个为主标签）
matched_by    = code | keyword | both
```

> 误判/漏判评估：M1 交付后抽样 100 条人工复核，把校准结果回写配置，形成闭环。

---

## 6. 技术选型

| 层 | 选型 | 理由 |
|----|------|------|
| 语言 | Python 3.12（本机 Anaconda 已就绪） | 生态成熟，爬虫/分析一体 |
| 采集 | `requests` + 重试/限速封装 | 当前站点无强反爬，无需 Playwright |
| 解析 | `beautifulsoup4` + `lxml` | 静态页解析高效，GBK 自动转码 |
| 存储 | SQLite（stdlib `sqlite3`） | 零运维、单文件、够用 |
| 报表 | `openpyxl`（Excel） + stdlib `csv` | 业务侧 Excel 查看最通用 |
| 调度 | macOS `launchd` / cron | 零依赖；程序自身幂等，重复触发无害 |
| 通知 | 飞书 Webhook（可选） | 接入团队现有 IM 生态 |

依赖极简：`requests`、`beautifulsoup4`、`lxml`、`openpyxl`，一份 `requirements.txt` 搞定。

---

## 7. 合规与反爬策略

1. **限速**：请求间隔 1~3 秒随机抖动，单次任务控制总时长
2. **重试退避**：5xx/超时指数退避重试（最多 3 次）；HTTP 403 立即熔断并告警，不硬闯
3. **UA 标识**：使用常规浏览器 UA；不伪造登录态
4. **数据边界**：仅采集公开投诉信息用于内部质量分析；不采集/不传播个人隐私字段（姓名、电话等出现于正文的默认脱敏处理可选）
5. **尊重版权**：数据仅内部使用，不做公开二次分发

---

## 8. 渐进式交付里程碑

| 里程碑 | 内容 | 验收标准 |
|--------|------|----------|
| **M1 POC**（本次交付） | 单文件爬虫：抓最新 N 页列表 → 解析 → 详情页全文 → 动力总成识别 → CSV 输出 | 跑通全流程，输出近 7 天动力总成抱怨 CSV，识别准确率抽检达标 |
| **M2 工程化** | 模块化目录 + SQLite 持久化 + 增量去重 + 配置外置 + 错误日志 | 重复运行不产生重复数据；配置改关键词无需改代码 |
| **M3 分析报表** | Excel 报表（品牌/车系/子系统汇总、趋势、TOP 排行）+ 飞书日报推送 | 每日自动产出可读报表 |
| **M4 扩展** | 历史回填基线、多源接入（缺陷中心/论坛）、网页看板（单文件 HTML，符合原型策略） | 多源数据合并，看板可交互 |

---

## 9. 目标目录结构（M2 起）

```
powertrain-complaints/
├── README.md
├── requirements.txt
├── config/
│   ├── settings.json          # 抓取参数（页数、限速、UA）
│   ├── issue_codes.json       # 代码→子系统映射
│   └── keywords.json          # 关键词白名单 / 排除黑名单
├── src/
│   ├── __init__.py
│   ├── crawler.py             # 采集：列表页 + 详情页
│   ├── parser.py              # 解析与清洗
│   ├── classifier.py          # 动力总成识别规则引擎
│   ├── storage.py             # SQLite 存取（幂等 upsert）
│   ├── report.py              # CSV / Excel 输出
│   └── main.py                # 编排入口（CLI 参数：--pages --since --full）
├── data/
│   └── complaints.db
└── output/
    └── reports/
```

---

## 10. 风险与对策

| 风险 | 影响 | 对策 |
|------|------|------|
| 页面改版导致解析失效 | 采集中断 | 解析器集中隔离；抓取失败自动告警；定期冒烟测试 |
| 反爬升级（403/封 IP） | 采集中断 | 限速 + 熔断 + 降级单线程；必要时升级 Playwright 方案 |
| 关键词误判/漏判 | 分析失真 | 双通道 + 代码优先；抽样人工复核闭环；规则配置化 |
| 投诉被站方删除/隐藏 | 历史数据缺口 | SQLite 保留历史快照，不做物理删除 |
| 新能源口径漂移（新故障模式） | 识别滞后 | 关键词表季度维护；保留原文便于回溯 |

---

## 11. 下一步

确认 §1 的 6 个决策点后，即进入 **M1 POC** 开发：
- 单文件 `collect.py`：抓列表（默认近 7 天页数）→ 详情页全文 → 双通道识别 → 输出 `output/powertrain_complaints.csv` + 控制台统计摘要
- 交付后附抽样人工复核结果，校准识别规则
