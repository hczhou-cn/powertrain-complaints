# 汽车动力总成抱怨采集分析系统 — 操作手册

> 适用对象：日常使用/维护本系统的同事（非开发者视角）
> 系统位置：`/Users/zhouhaocheng/powertrain-complaints/`
> 更新日期：2026-08-13

---

## 1. 系统简介

本系统每天自动从**车质网**（12365auto.com，国内最权威的汽车投诉平台）抓取中国市场汽车投诉，自动识别其中**动力总成**相关抱怨（发动机 / 变速器 / 离合器 / 动力电池 / 驱动电机 / 电控 / 混动系统），完成以下闭环：

```
自动采集 → 智能识别分类 → 数据入库 → 生成报表 → 推送企业微信群
```

**系统能回答的问题：**
- 最近有哪些动力总成投诉？涉及哪些品牌/车系？
- 哪个品牌/车系/子系统问题最集中？
- 每天投诉量趋势如何？
- 哪些是高风险问题（自燃/失速/无法启动/刹车失灵）？

**当前数据规模：** 1 个月基线约 3,835 条动力总成投诉（2026-07-12 起）。

---

## 2. 快速上手（30 秒）

```bash
cd /Users/zhouhaocheng/powertrain-complaints

# 手动采集一次 + 生成报表 + 推送到群
python3 run.py --notify --dashboard
```

完成后：
- 控制台打印统计摘要（总数、动力总成数、子系统分布、TOP 品牌）
- 报表文件生成在 `output/reports/`
- 企微群收到日报 + Excel + 看板，飞书群收到卡片

---

## 3. 日常操作

### 3.1 手动采集

```bash
cd /Users/zhouhaocheng/powertrain-complaints

python3 run.py                      # 采集近 7 天 + 生成 CSV/Excel 报表
python3 run.py --notify --dashboard # 采集 + 报表 + 推送企微/飞书 + 生成看板
```

**增量原理**：系统只抓"新出现"的投诉，重复运行不会产生重复数据；通常几秒到几分钟完成。

### 3.2 查看报表（三种形态）

| 形态 | 文件位置 | 查看方式 |
|------|----------|----------|
| Excel 报表 | `output/reports/powertrain_report_日期.xlsx` | Excel/WPS 打开，6 个工作表 |
| HTML 看板 | `output/reports/dashboard_日期.html` | 浏览器打开（双击即可） |
| CSV 明细 | `output/reports/powertrain_complaints_日期.csv` | Excel 或任何文本工具 |

**Excel 报表 6 个 Sheet：**

| Sheet | 内容 |
|-------|------|
| 概览 | 核心指标、子系统分布、TOP 10 品牌、⚠ 高风险问题 |
| 每日趋势 | 每天投诉量柱状图 |
| 品牌排行 | 各品牌动力总成投诉数 |
| 车系排行 | 各车系投诉数 TOP 50 |
| 子系统分布 | 发动机/变速器/电池等占比 |
| 投诉明细 | 全部明细，**高风险行标红** |

**HTML 看板**：指标卡 + 4 张交互图表（趋势/子系统/品牌/车系）+ 高风险列表 + 可搜索明细表。

### 3.3 定时任务（已配置，一般无需操作）

每天 **09:00** 自动采集 + 推送，无需人工干预。

- 查看定时任务：`crontab -l`
- 查看运行日志：`tail -f /Users/zhouhaocheng/powertrain-complaints/logs/cron.log`
- 修改推送内容/时间：编辑 crontab（见第 6 节）

### 3.4 历史数据回填

需要更长历史基线时（一次性操作，约 10-20 分钟/月）：

```bash
# 回填最近 3 个月（默认只抓列表字段，足够做趋势分析）
python3 run.py --backfill-months 3 --direct
```

> `--direct` 直连模式：实测绕过本地代理更稳定（源站无强反爬，合规限速下可直连）。

---

## 4. 通知渠道配置

系统支持两个推送渠道，均已启用。

### 4.1 修改企微 webhook（机器人换群/重建时）

**方法一（推荐）：编辑配置文件**

```bash
cd /Users/zhouhaocheng/powertrain-complaints
open config/notify.json    # 用文本编辑器打开
```

找到 `wecom` 段，替换 `webhook` 值保存：

```json
"wecom": {
  "webhook": "https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=新key",
  "enabled": true,
  "send_report": true,
  "send_dashboard": true
}
```

保存即生效，验证：
```bash
python3 run.py --report-only --notify     # 发一条测试日报
```

**方法二：环境变量（临时切换，优先级更高）**

```bash
WECOM_WEBHOOK="https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=新key" \
  python3 run.py --report-only --notify
```

> ⚠️ 优先级：环境变量 > 配置文件。**长期修改请改配置文件**（cron 定时任务不加载 shell 环境变量，只读配置文件）。

### 4.2 飞书 webhook 修改

同上，改 `config/notify.json` 顶部的 `webhook` 字段，或环境变量 `FEISHU_WEBHOOK`。

### 4.3 推送开关速查

| 配置项（config/notify.json） | 作用 |
|------------------------------|------|
| `enabled`（飞书顶层） | 飞书日报开关 |
| `wecom.enabled` | 企微日报开关 |
| `wecom.send_report` | 企微推 Excel 报表 |
| `wecom.send_dashboard` | 企微推 HTML 看板 |

命令行临时开关：`--notify` 全部开启、`--no-notify` 全部关闭、`--dry-run` 只预览不发送。

---

## 5. 识别规则维护（业务侧最常用）

系统自动识别"动力总成"抱怨，规则全部外置在 `config/` 目录，**业务可直接修改，无需改代码**。

### 5.1 关键词维护（config/keywords.json）

| 配置项 | 说明 | 示例 |
|--------|------|------|
| `subsystem_keywords` | 各子系统专有词（定类用） | 发动机：["发动机","烧机油","增压器"...] |
| `generic_keywords` | 通用动力总成词（单独命中即判定） | 失速、无法启动、熄火 |
| `context_keywords` | 条件词（需与子系统词共现，防误报） | 异响、漏油、抖动 |
| `exclude_keywords` | 排除词（销售服务类误报过滤） | 不予兑现、订金、锁单 |
| `high_risk_keywords` | 高风险告警词（日报告警用） | 自燃、失速、刹车失灵 |

**举例：** 市场出现新型故障叫"增程器异响"，想纳入监控：
1. 编辑 `config/keywords.json`
2. 在 `subsystem_keywords` 的"混动系统"数组里加 `"增程器"`
3. 保存后执行重打标：
```bash
python3 run.py --reclassify
```

### 5.2 问题代码映射（config/issue_codes.json）

车质网每条投诉带"典型问题代码"（如 A191=发动机、H343=动力电池）：

```json
{
  "code_prefix_map": { "A": "发动机", "B": "变速器", "C": "离合器" },
  "code_whitelist": { "H340": "动力电池", "H343": "动力电池/BMS" }
}
```

新代码确认为动力总成时，加入 `code_whitelist`（精确匹配，更可靠）。

---

## 6. 定时任务管理

### 6.1 查看当前任务

```bash
crontab -l
```

### 6.2 修改推送时间/参数

```bash
crontab -e
```

当前任务行（每日 09:00）：
```bash
0 9 * * * cd /Users/zhouhaocheng/powertrain-complaints && /opt/anaconda3/bin/python3 run.py --notify --dashboard >> /Users/zhouhaocheng/powertrain-complaints/logs/cron.log 2>&1
```

| 改动需求 | 修改方法 |
|----------|----------|
| 改时间（如 08:30） | 行首 `0 9` → `30 8` |
| 不推送只采集 | 去掉 `--notify --dashboard` |
| 改回溯天数（如近 30 天） | 加 `--days 30` |

> cron 语法：`分 时 日 月 周 命令`，如 `0 9 * * *` = 每天 9:00。

---

## 7. 数据分析（进阶：SQL 查询）

数据存在 `data/complaints.db`（SQLite 单文件），可用任意 SQLite 工具（如 DB Browser for SQLite）打开，或命令行：

```bash
cd /Users/zhouhaocheng/powertrain-complaints
python3 -c "
import sqlite3
conn = sqlite3.connect('data/complaints.db')
# 近 7 天 TOP 车系
for row in conn.execute(\"SELECT brand, series, COUNT(*) FROM complaints WHERE complaint_date >= date('now','-7 day') AND is_powertrain=1 GROUP BY brand, series ORDER BY COUNT(*) DESC LIMIT 10\"):
    print(row)
"
```

**常用字段：** `complaint_no`（编号）、`brand`（品牌）、`series`（车系）、`title`（标题）、`content`（正文）、`issue_code`（问题代码）、`complaint_date`（日期）、`is_powertrain`（是否动力总成）、`pt_subsystem`（子系统）、`matched_keywords`（命中词）。

---

## 8. 常见问题排查

| 现象 | 原因与处理 |
|------|-----------|
| 群里没收到日报 | ① 检查 `config/notify.json` 的 `enabled` 和 webhook 是否还在 ② 机器人是否被移出群 ③ 看 `logs/cron.log` 有无"推送成功" ④ 手动跑 `python3 run.py --report-only --notify` 验证 |
| 企微提示"机器人被移出" | 机器人被清理/群重建，按第 4.1 节换 webhook |
| 日报内容为空/数量为 0 | 数据源当天无新增或未采集到；手动跑一次 `python3 run.py` 看日志 |
| 运行日志报"403/超时" | 车质网偶发限流，程序自动重试+熔断，属正常；连续多次失败可稍后重跑 |
| 改了关键词不生效 | 改完需执行 `python3 run.py --reclassify` 重新打标 |
| Excel 打开乱码 | CSV 是 utf-8-sig 编码，Excel 直接打开正常；若用旧版 Excel 请用"数据→从文本导入"并选 UTF-8 |
| 看板打不开/图表空白 | 看板图表走 CDN（需联网）；请用 Chrome/Edge 打开 |
| 想临时停掉推送 | `python3 run.py --no-notify`（仅本次），或把配置文件 `enabled` 改 `false`（长期） |

### 日志查看

```bash
tail -f logs/collect.log      # 采集运行日志（每次运行）
tail -f logs/cron.log         # 定时任务日志
```

---

## 9. 安全与合规说明

- **数据来源**：车质网公开投诉信息，仅用于**内部质量分析**，不得对外二次分发
- **webhook 密钥**：`config/notify.json` 含群机器人地址，**不要**发送到外部或提交到公开仓库（该文件已被 git 忽略）
- **采集合规**：系统已内置请求限速（1~2.5 秒随机间隔）、失败重试、反爬熔断，请勿自行调高频率
- **个人隐私**：投诉正文可能含个人信息，分析使用时注意脱敏，不外传

---

## 10. 附录

### 10.1 目录结构

```
powertrain-complaints/
├── run.py                    # 入口命令（所有操作都通过它）
├── config/                   # ⭐ 业务可维护的配置（改这里）
│   ├── settings.json         #   采集参数/路径
│   ├── issue_codes.json      #   问题代码映射
│   ├── keywords.json         #   关键词规则 + 高风险词
│   └── notify.json           #   飞书/企微 webhook
├── src/                      # 程序源码（一般不需要动）
├── data/complaints.db        # 数据仓库（SQLite）
├── output/reports/           # 报表产物（CSV/Excel/看板）
└── logs/                     # 运行日志
```

### 10.2 常用命令速查

| 目的 | 命令 |
|------|------|
| 日常采集+推送 | `python3 run.py --notify --dashboard` |
| 只看报表不采集 | `python3 run.py --report-only` |
| 回填历史 3 个月 | `python3 run.py --backfill-months 3 --direct` |
| 改规则后重打标 | `python3 run.py --reclassify` |
| 预览推送内容 | `python3 run.py --report-only --notify --dry-run` |
| 查看定时任务 | `crontab -l` |
| 查看日志 | `tail -f logs/collect.log` |
